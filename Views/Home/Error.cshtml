@model ErrorViewModel
@{
    ViewData["Title"] = "Lỗi";
}

<div class="container text-center my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-5">
                    <i class="bi bi-exclamation-triangle text-warning display-1 mb-4"></i>
                    <h1 class="display-4 mb-4">Đã xảy ra lỗi</h1>
                    
                    @if (Model.ShowRequestId)
                    {
                        <p class="text-muted mb-4">
                            Mã lỗi: <code>@Model.RequestId</code>
                        </p>
                    }

                    <p class="lead mb-4">
                        Xin lỗi vì sự bất tiện này. Vui lòng thử lại sau hoặc liên hệ với chúng tôi nếu vấn đề vẫn tiếp tục.
                    </p>

                    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                        <a href="/" class="btn btn-outline-dark btn-lg px-4 gap-3">
                            <i class="bi bi-house-door me-2"></i>Về trang chủ
                        </a>
                        <a href="mailto:support@example.com" class="btn btn-outline-secondary btn-lg px-4">
                            <i class="bi bi-envelope me-2"></i>Liên hệ hỗ trợ
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 